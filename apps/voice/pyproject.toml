[project]
name = "voice"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = []

[project.scripts]
voice = "voice:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"

[dependency-groups]
dev = [
    "ruff>=0.16.9",
]
